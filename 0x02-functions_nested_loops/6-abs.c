#include "main.h"
/**
 * _abs - Returns the absolute value of an integer
 * @num: the integer being checked
 * Return: the absolute value of int
 */
int _abs(int num)
{
	if (num >= 0)
	{
		return (num);
	}
	return (-num);
}
